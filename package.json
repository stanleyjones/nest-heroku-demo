{
  "name": "nest-heroku-demo",
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start:prod --workspace=server"
  }
}
