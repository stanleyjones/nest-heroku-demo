{
  "name": "nest-heroku-demo",
  "version": "0.1.0",
  "workspaces": [
    "api",
    "client"
  ],
  "scripts": {
    "start": "npm start --workspace=api",
    "build": "npm run build --workspace=client"
    "start:prod": "npm run build --workspace=client && npm run start:prod --workspace=client"
  }
}
